<%= form_for(@tag_image) do |f| %>
  <% if @tag_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_image.errors.count, "error") %> prohibited this tag_image from being saved:</h2>

      <ul>
      <% @tag_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br />
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
